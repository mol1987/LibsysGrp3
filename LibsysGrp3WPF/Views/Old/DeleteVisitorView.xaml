<UserControl x:Class="LibsysGrp3WPF.Views.DeleteVisitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibsysGrp3WPF.Views"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <DockPanel>
                <ListBox  ItemsSource="{Binding UsersList, Mode=OneWay}" 
                      SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}" />
                <Button Content="Remove" Command="{Binding ButtonRemove}" Margin="5,10"/>
            </DockPanel>

        </ScrollViewer>


    </Grid>
</UserControl>